// set global manipulation vars
var scope = this;
var view = this.project.view;
window.paperLassoScope['scope'] = scope;

// needs to register functions in the global vars that can be called from the main script.
window.paperLassoScope['some_function'] = function (somevars) {
	// var path...
	// do something with path.
}

function onMouseDown(event) {

}

function onMouseDrag(event) {

}

function onMouseUp(event) {

}
